<view wx:for="{{feedback_data}}" wx:key="time">
    <view class="cu-card dynamic {{isCard?'no-card':''}}">
    <view class="cu-item shadow">
    <view class="cu-list menu-avatar">
      <view class="cu-item">
        <!-- 这里未来插入用户头像 -->
        <view class="cu-avatar round lg" style="background-image:url({{item.avatar}});"></view>
        <view class="content flex-sub">
          <view>{{item.nickname?item.nickname:item.stu_id}}</view>
          <view class="text-gray text-sm flex justify-between">
            {{item.time}}
          </view>
        </view>
      </view>
    </view>
    <view class="text-content" data-item="{{item}}" bindtap="jumpToDetail">
        <text user-select>{{item.message}}</text>
    </view>
    <!-- <view class="grid flex-sub padding-lr {{isCard?'col-3 grid-square':'col-1'}}">
      <view class="bg-img {{isCard?'':'only-img'}}" style="background-image:url(https://ossweb-img.qq.com/images/lol/web201310/skin/big10006.jpg);" wx:for="{{isCard?9:1}}" wx:key="{{index}}">
      </view>
    </view> -->
    <!-- <view class="text-gray text-sm text-right padding">
      <view wx:if="{{isAgree}}">
        <text class="cuIcon-attentionfill margin-lr-xs"></text> {{item.view?item.view:0}}
        <text class="cuIcon-appreciate margin-lr-xs"></text> {{item.agree?item.agree:0}}
      </view>
      <view wx:else>
        <text class="cuIcon-attentionfill margin-lr-xs"></text> {{item.view?item.view:0}}
        <text class="cuIcon-appreciatefill margin-lr-xs"></text> {{item.agree?item.agree:0}}
      </view>
      <!-- <text class="cuIcon-messagefill margin-lr-xs"></text> {{}} -->
    <!-- </view> -->
    </view>
    </view>
</view>

<!-- <movable-area bindtap="addAgree" class="movable-area">
    <movable-view class="movable-view" direction="all">
    <image src="../../../../res/icons/agree.png"></image>
    </movable-view>
</movable-area> -->