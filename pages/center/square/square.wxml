<view>
    <view class="nav">
        <scroll-view scroll-x class="bg-white nav text-center">
            <view class="cu-item {{index === curr_category?'text-blue cur':'text-black'}}" wx:for="{{category_list}}" wx:key="index" bindtap="selectTerm" data-id="{{index}}">
                {{item}}
            </view>
        </scroll-view>
    </view>

    <button bindtap="sendTest"></button>

</view>